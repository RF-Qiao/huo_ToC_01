# Components

Shared, reusable UI components should live here once the prototype screens are extracted from `src/prototype`.

For the next pass, good candidates are:

- `BottomNav`
- `StatusDialog`
- `Toast`
- `LoadingIndicator`
- `EmptyState`
- ticket/order cards
- hero cards
